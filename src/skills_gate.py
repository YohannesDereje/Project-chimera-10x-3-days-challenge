from __future__ import annotations
from typing import Dict


class MediaDownloader:
    """Stub MediaDownloader skill.

    `run(payload)` accepts input per the skill contract and returns an empty dict.
    """

    def run(self, payload: Dict) -> Dict:
        return {}


class Transcriber:
    """Stub Transcriber skill.

    `run(payload)` accepts input per the skill contract and returns an empty dict.
    """

    def run(self, payload: Dict) -> Dict:
        return {}
